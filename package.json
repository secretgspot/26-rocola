{
	"name": "26-rocola",
	"private": true,
	"version": "0.0.1",
	"type": "module",
	"scripts": {
		"dev": "vite dev",
		"build": "vite build",
		"preview": "vite preview",
		"prepare": "svelte-kit sync || echo ''",
		"check": "svelte-kit sync && svelte-check --tsconfig ./jsconfig.json",
		"check:watch": "svelte-kit sync && svelte-check --tsconfig ./jsconfig.json --watch",
		"db:push": "drizzle-kit push",
		"db:generate": "drizzle-kit generate",
		"db:migrate": "drizzle-kit migrate",
		"db:studio": "drizzle-kit studio",
		"stripe:listen": "docker run --rm -it -v $HOME/.config/stripe:/root/.config/stripe --add-host=host.docker.internal:host-gateway stripe/stripe-cli listen --forward-to host.docker.internal:5173/api/webhooks/stripe",
		"test:integration": "vitest run test/integration --config vitest.config.js",
		"test": "vitest"
	},
	"devDependencies": {
		"@sveltejs/adapter-vercel": "^6.3.1",
		"@sveltejs/kit": "^2.50.1",
		"@sveltejs/vite-plugin-svelte": "^6.2.4",
		"@types/better-sqlite3": "^7.6.13",
		"@types/node": "^22",
		"drizzle-kit": "^0.31.8",
		"drizzle-orm": "^0.45.1",
		"svelte": "^5.48.2",
		"svelte-check": "^4.3.5",
		"typescript": "^5.9.3",
		"vite": "^7.3.1",
		"vite-plugin-devtools-json": "^1.0.0",
		"vitest": "^1.0.0"
	},
	"dependencies": {
		"better-sqlite3": "^12.6.2",
		"open-props": "^1.7.17",
		"stripe": "^20.3.0",
		"ws": "^8.19.0"
	}
}
